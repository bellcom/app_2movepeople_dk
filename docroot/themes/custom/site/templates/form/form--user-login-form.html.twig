{#
/**
 * @file
 * Default theme implementation for a 'form' element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the form.
 *
 * @see template_preprocess_form()
 *
 * @ingroup themeable
 */
#}
{{ attach_library('bc_2movepeople_dashboard/bc_2movepeople_dashboard.user-tasks') }}
<div class="custom-login-form">
  <form{{ attributes }}>
    {{ children }}
  </form>
  <br/><br/>
  <a href="{{ path('bc_2movepeople_dashboard.users.create') }}"
   class="btn btn-secondary use-ajax" data-dialog-type="modal" data-dialog-options='{"width":640}'
   role="button">{{ 'Create Account'|trans }}</a>
</div>
