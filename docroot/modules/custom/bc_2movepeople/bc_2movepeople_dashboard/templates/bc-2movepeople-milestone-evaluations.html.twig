{#

/**
 * @file
 * Template file for getMilestoneEvaluations.
 */
#}
{{ attach_library('bc_2movepeople_dashboard/bc_2movepeople_dashboard.user-tasks') }}

<div class="milestone-evaluation">
  {% for item in milestones_data %}
    <p>{{ item.milestone.name }}</p>
    <h3>{{ 'Evaluering' |trans}}</h3>
    <p>{{ item.milestone.purpose }}</p>
    <ul>
      {% for target in item.targets %}
        <li><p>{{ target.name }}</p>{{ target.evaluation }}</li>
          {% endfor %}
    </ul>
    <a href="{{ path('bc_2movepeople_dashboard.milestone.evaluate', {'node': item.milestone.id})}}" id="btn-evaluering" data-dialog-type="modal" class="btn btn-default use-ajax">{{ 'Edit'|trans }}</a>
  {% endfor %}
  <div class="milestone-evaluation-footer">
    <a href="{{ path('bc_2movepeople_dashboard.milestone.evaluations.pdf', {'user': user})}}" id="btn-evaluering" class="btn btn-default">{{ 'Save as PDF'|trans }}</a>
  </div>
</div>